# The product release this deploy-repo version pins. Moved by git pull on
# upgrade; override in your own .env only to roll back (the installer will
# warn loudly). NO v prefix — image tags are bare semver.
OPSROOM_TAG=0.1.1
OPSROOM_REGISTRY=ghcr.io/algoradev
